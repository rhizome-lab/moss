[package]
name = "rhizome-moss-cli-parser"
version = "0.1.0"
edition = "2024"
description = "Parse CLI --help output from various frameworks"
license = "MIT"

[dependencies]
regex = "1"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
insta = "1"
